---
name: x-conventions
description: Check this user's personal-preference wiki before naming a git branch, writing a commit or opening a PR, running a jest-backed test command, relying on npm's allowScripts script-approval, starting work on a Bun/TypeScript project, ordering items in code (imports, keys, flags), or treating an empty x-memory query result as proof of no history. Use every time one of these comes up, not only at session start.
user-invocable: false
---

# x-conventions

1. Identify which topic above the current action matches.
2. Read `~/.cache/agents/memory/index.md` (global) and, if the project
   has one, `<project>/.agents/x-memory/index.md` — unless already
   read this session for this exact topic.
3. Follow the link matching the topic, apply what it says.
4. No matching page → nothing to check, proceed normally.
